# Screenshot sources

Raw popup captures. The landing gallery uses `website/public/screenshots/features/` instead.

`assets-send.png` is the UI crop on the send social billboard. The other files here are leftovers from the old split-panel site cards — do not regenerate those cards.
